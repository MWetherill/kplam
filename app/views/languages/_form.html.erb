<%= form_with(model: @language, class: "w-100 w-lg-50") do |form| %>
  <div class="mb-3">
    <%= form.text_field :title, placeholder: "Title", class: "form-control" %>
  </div>
  <%= form.submit "Submit", class: "btn btn-primary" %>
<% end %>